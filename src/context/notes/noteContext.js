import { createContext } from "react";

const noteContext = createContext();      // new context is created named as noteContext
export default noteContext;
// In this component we have just created a context nothing else